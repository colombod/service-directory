Use `EmptyState` when a list has no rows — never a spinner-shaped placeholder or an illustration.

```jsx
<EmptyState title="No services configured" hint="Add services to your config file, or register one below." />
```

Copy is factual and tells the user the next concrete action.
