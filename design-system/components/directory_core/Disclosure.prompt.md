Use `Disclosure` for a secondary form that should not compete with the catalogue — in the product it wraps the add-service form at the bottom of the viewer's default pane.

```jsx
<Disclosure title="Register a service">{form}</Disclosure>
```

The marker is `+` closed, `−` (U+2212) open, in `--accent`. Never a chevron.
